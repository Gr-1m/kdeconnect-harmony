# C++ objects and libs

# Qt-es
# Qt unit tests

# QtCreator

# QtCreator Qml

# QtCreator CMake


# Agent-Temp-Use MarkDown
AGENTS1.md
EMULATOR_NOTES.md
KICKOFF_PROMPT.md
docs/
.idea/

# syncthing
.stfolder/

# HarmonyOS / OpenHarmony 工程产物与机器本地文件（跨机排除清单见 .stignore）
build/
oh_modules/
.cxx/
.hvigor/
.preview/
.clangd
.clang-tidy
local.properties
sign/
*.log

# 多 agent 协作消息（随 Syncthing 同步给 Win10，不入 git）
AgentsConversion/

# 本机缓存与工具状态（AtomCode REVIEW §1.3，机器本地）
.cache/
.codeartsdoer/
.appanalyzer/
wecode-cpp.db

# 临时参考快照（会随演进过期，正本保留在旧工作区 PreDev）
devdocs/reference/

# Syncthing 本机忽略配置（机器本地，Win10 侧手动镜像）
.stignore

# DevEco 验证产物与 IDE 重写前的备份（CodeArts MSG70 裁决，不入库）
.deveco-verify/
build-profile.json5.tmpbak
.vtmp/

# Rust cargo 构建产物（R1：host 侧统一构建到 /tmp，这里兜底）
entry/src/main/cpp/rust/kdc_core/target/

# Syncthing 冲突副本（2026-09-13 事故：双机同写同名文件产生；机器本地，不入库）
.sync-conflict-*

# 构建产物一类（PROCESS §9.1：与 .stignore 对应；Rust cargo target/）
target/
# 代码索引缓存（PROCESS §9.1，机器本地）
.codegraph/
